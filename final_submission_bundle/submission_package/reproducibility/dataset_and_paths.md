# Dataset and Paths (No Dataset Included)

## SemanticKITTI expected path

The project expects SemanticKITTI under:

- `/home/ai25mtech14009/rangevit-main/rangevit-main/data/semantic_kitti/dataset/sequences/`

Pass this parent path as `--data_root` during training/evaluation.

## Important policy

- Include only instructions and folder structure.
- Do NOT include raw dataset in submission.

## Example check

```bash
ls /home/ai25mtech14009/rangevit-main/rangevit-main/data/semantic_kitti/dataset/sequences
```
